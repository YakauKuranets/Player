# Python dependencies for the PLAYE PhotoLab desktop backend.
# This list is based on the development plan in Инструкция_Desktop_AI.md.
# It includes FastAPI and supporting libraries as well as the deep learning
# frameworks required for the AI models. The versions are pinned for
# reproducibility. Feel free to adjust these versions if installation
# issues arise on specific platforms (e.g. Windows vs. Linux).

fastapi==0.108.0
uvicorn[standard]==0.25.0
python-multipart==0.0.6

# PyTorch and vision libraries. On systems without CUDA a CPU-only build
# of torch/torchvision can be used instead of the GPU-enabled versions.
torch==2.1.0
torchvision==0.16.0
onnxruntime-gpu==1.16.0

# Image processing utilities
opencv-python==4.8.1.78
numpy==1.26.2
Pillow==10.1.0

# Misc utilities
requests==2.31.0
tqdm==4.66.1